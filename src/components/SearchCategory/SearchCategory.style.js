import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  category_container: {
    borderRadius: 10,
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginLeft: 10,
    marginRight: 10,
    marginTop: 30,
  },
});
